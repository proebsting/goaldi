# demo cpu time measurement

procedure main() {
	local tmid := 0.001
	local tmax := 5
	while tmid < tmax do {
		tmid *:= &phi
		while cputime() < tmid & cputime() < tmax do
			{}
		write("CPU time: ", cputime())
	}
}
